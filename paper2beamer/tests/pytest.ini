[pytest]
# e2e tests download Docling models and need xelatex; excluded by default so the
# normal suite stays fast and hermetic. Run them explicitly with: -m e2e
markers =
    e2e: end-to-end tests needing docling + xelatex (opt-in; downloads models)
addopts = -m "not e2e"
